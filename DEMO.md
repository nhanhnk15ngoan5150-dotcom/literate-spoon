# DEMO

## 一、Demo 目的

本 Demo 通过三个实际业务问题，验证 AI 数据助手的数据查询、自然语言理解和结果解释能力。

测试问题：

1. 哪个品类的营业额最高？
2. 牛肉Poke在六月的销售额是多少？
3. 最近客单价是上涨还是下降？

同时增加一个开发过程中发现的异常案例：

4. 什么饮品卖得最好？

重点验证：

* AI 是否能够正确理解自然语言问题
* 查询条件是否正确
* 查询结果是否来自真实业务数据
* LLM 是否会产生数据幻觉
* 多轮对话是否会错误继承上一轮上下文
* 出现错误后能否通过数据和代码定位问题

---

## 二、问题一：哪个品类的营业额最高？

### 用户问题

```text
哪个品类的营业额最高？
```

### 查询逻辑

系统首先识别用户的问题属于：

```text
商品品类营业额排名
```

随后根据商品表中的品类信息与销售数据进行关联，并按照商品品类进行聚合：

```text
sales
  ↓
关联 products
  ↓
按照 product_category 分组
  ↓
SUM(amount)
  ↓
ORDER BY 营业额 DESC
```

最终将数据库查询结果交给 LLM，由 LLM 负责组织成自然语言回答。

### 为什么数字可信？

数字不是由 LLM 自己生成，而是由数据库统计得到。

整体链路为：

```text
原始 CSV
   ↓
数据清洗
   ↓
SQLite
   ↓
SQL 聚合查询
   ↓
查询结果
   ↓
LLM 进行自然语言解释
```

因此，如果对结果存在疑问，可以直接重新执行对应 SQL，对结果进行复核。

---

## 三、问题二：牛肉Poke在六月的销售额是多少？

### 用户问题

```text
牛肉Poke在六月的销售额是多少？
```

### 系统需要识别的条件

```text
商品：牛肉Poke
时间：6月
指标：销售额
```

查询时先根据商品名称定位对应的 `product_id`，再根据销售日期筛选六月的数据，最后对销售金额进行聚合。

逻辑可以概括为：

```text
商品 = 牛肉Poke
        +
日期属于6月
        ↓
SUM(amount)
```

### 为什么可信？

商品基础数据中存在明确的牛肉Poke商品记录。

销售数据包含：

```text
order_id
date
store_id
product_id
qty
amount
payment
```

因此销售额可以进一步追溯到具体销售记录，而不是由模型自行估算。

---

## 四、问题三：最近客单价是上涨还是下降？

### 用户问题

```text
最近客单价是上涨还是下降？
```

### 计算方式

项目中将客单价定义为：

```text
客单价 = 营业额 / 订单量
```

系统先按照时间区间统计营业额和订单量，再计算对应时间区间的客单价，并进行对比。

```text
时间区间 A
营业额 / 订单量
      ↓
客单价 A

时间区间 B
营业额 / 订单量
      ↓
客单价 B

客单价 A vs 客单价 B
      ↓
上涨 / 下降
```

因此最终的上涨或下降结论来源于实际统计结果，而不是 LLM 根据语言习惯进行猜测。

---

# 五、异常案例：饮品问题

开发过程中，我专门测试了一个容易暴露 LLM 上下文理解问题的问题。

### 第一次提问

```text
什么饮品卖得最好？
```

AI 曾经错误回答：

```text
牛肉Poke卖得最好。
```

这个结果明显存在业务语义问题。

### 如何发现？

首先检查商品基础数据：

```text
商品：牛肉Poke
品类：主食
```

因此：

```text
牛肉Poke ≠ 饮品
```

也就是说，即使 AI 返回的回答在语言上看起来正常，但从业务数据来看是错误的。

这说明不能只依赖 LLM 的自然语言回答，还需要结合数据库结果和业务字段进行验证。

---

# 六、问题定位

继续测试：

```text
老板：
什么饮品卖得最好？
```

在上一轮已经出现“牛肉Poke”的情况下，模型容易受到上一轮实体信息影响，把上一轮的商品错误继承到当前问题。

因此问题并不是数据库中的数据错误，而是：

```text
上一轮上下文
      ↓
牛肉Poke
      ↓
下一轮问题：什么饮品卖得最好？
      ↓
错误继承商品实体
```

这类问题属于多轮对话中的上下文串题。

---

# 七、修复方式

针对这个问题，对查询条件的识别进行了调整。

当用户明确提出：

```text
什么饮品卖得最好？
```

系统应优先根据当前问题确定查询条件：

```text
查询类型：商品排名
商品类别：饮品
```

然后只在符合“饮品”类别的商品范围内进行查询。

逻辑变为：

```text
当前用户问题
      ↓
识别查询条件
      ↓
商品类别 = 饮品
      ↓
数据库筛选饮品商品
      ↓
统计销量/销售额
      ↓
排序
      ↓
LLM解释结果
```

而不是：

```text
上一轮实体
      ↓
直接继承
      ↓
生成答案
```

修复后再次测试该问题，可以避免将“牛肉Poke”这样的非饮品商品错误带入当前查询。

---

# 八、Demo 最终验证的核心

这个项目并不是试图证明：

```text
AI 永远不会犯错
```

而是希望证明：

```text
AI 可以犯错
    ↓
但错误可以被发现
    ↓
可以通过数据库和业务规则验证
    ↓
可以定位到具体问题
    ↓
可以进行代码层修复
```

因此系统的数据可信链路设计为：

```text
用户自然语言问题
        ↓
AI 理解问题
        ↓
确定查询条件
        ↓
数据库执行事实查询
        ↓
返回结构化结果
        ↓
LLM 进行自然语言解释
```

而不是：

```text
用户问题
   ↓
LLM直接猜答案
```

本 Demo 最终希望证明的是：

> **LLM 负责理解和解释，数据库负责提供事实。** 


当两者职责分离后，可以显著降低 AI 在业务数据分析场景中的数字幻觉和事实错误风险。

 **牛马负责PUA牛马**  
 
 **中国牛马>野生狗奶**  **保质期**
