# AI_USAGE.md

## 一、使用的 AI 工具

本项目开发过程中主要使用了 AI 编程工具辅助完成：

* Claude Code  **屁，这个是ai写的，我可没有用，额度转瞬即逝，俺是山里人**  **中国人不骗中国人**
* ChatGPT

AI 主要用于：

* 需求理解
* 代码结构设计
* 数据处理代码辅助
* FastAPI 接口开发
* React 页面开发
* SQL 查询逻辑分析
* Bug 定位
* Prompt 调整
* 代码审查和重构建议

AI 主要承担辅助开发角色，最终代码是否采用、数据是否正确以及业务逻辑是否合理，由人工进行验证。

---

## 二、如何拆解任务

项目没有将整个需求一次性丢给 AI，而是按照功能拆分。

大致拆解为：

```text
题目分析
   ↓
数据审计
   ↓
数据清洗
   ↓
数据库设计
   ↓
后端 API
   ↓
前端数据看板
   ↓
AI 数据助手
   ↓
AI 查询准确性测试
   ↓
Bug 定位
   ↓
最终文档
```

例如：

### 第一阶段：数据

先让 AI 分析 CSV 数据结构和数据质量问题。

### 第二阶段：后端

让 AI 根据已经确定的数据结构设计 FastAPI 接口。

### 第三阶段：前端

根据 API 返回的数据设计 React 数据看板。

### 第四阶段：AI 助手

再单独处理自然语言查询和 LLM 调用，而不是让 LLM 直接生成业务数字。

### 第五阶段：测试

通过真实业务问题测试 AI 返回结果，并使用数据库进行二次验证。

---

## 三、一个真实的 Prompt 示例

例如在开发 AI 数据助手时，使用过类似下面的任务拆解：

```text
我现在需要给这个餐饮经营数据看板增加一个 AI 数据助手。

要求：
1. 用户可以使用自然语言询问销售数据。
2. AI 不能直接编造数字。
3. 查询结果必须来自数据库。
4. 如果用户的问题涉及商品、门店、时间或商品分类，需要正确识别查询条件。
5. LLM 只负责理解问题和解释查询结果。
6. 如果无法确定查询条件，不要自行编造。
7. 请先分析当前 main.py 的接口结构，再提出最小改动方案。
8. 修改时不要破坏现有数据看板 API。
```

这种方式比直接要求：

```text
帮我把整个项目写完
```

更容易控制代码质量，也更容易发现 AI 的错误。

---

# 四、AI 出现错误的实际案例

开发过程中遇到过一个比较典型的问题。

用户连续提出：

```text
什么饮品卖得最好？
```

AI 返回：

```text
牛肉Poke卖得最好。
```

这个答案从语言上看似乎没有问题，但业务上明显不合理。

于是没有直接接受 AI 的答案，而是回到商品基础数据进行检查。

查询商品信息后发现：

```text
牛肉Poke
product_category = 主食
```

因此：

```text
牛肉Poke ≠ 饮品
```

说明 AI 的回答存在业务语义错误。

---

# 五、如何发现 AI 的错误

发现这个问题主要依靠三层验证。

### 第一层：业务常识验证

“饮品”与“牛肉Poke”本身存在明显语义冲突。

### 第二层：数据库验证

检查商品基础数据：

```text
product_name = 牛肉Poke
product_category = 主食
```

确认不是数据分类本身错误。

### 第三层：代码逻辑验证

继续检查多轮对话的查询逻辑，发现上一轮已经出现的商品实体可能被错误继承到了下一轮问题。

因此最终定位为：

```text
多轮上下文实体继承问题
```

而不是数据库数据问题。

---

# 六、如何修改

修复思路不是让模型“记住牛肉Poke不是饮品”，而是调整查询逻辑。

当用户明确询问：

```text
什么饮品卖得最好？
```

首先重新识别当前问题中的：

```text
商品类别 = 饮品
```

然后数据库查询应该限定在饮品商品范围内。

即：

```text
当前问题
   ↓
重新识别查询条件
   ↓
商品类别 = 饮品
   ↓
数据库筛选
   ↓
统计排名
   ↓
返回结果
```

而不是：

```text
上一轮商品
   ↓
直接继承
   ↓
生成答案
```

这次问题让我意识到，多轮 AI 数据查询不能只依赖 LLM 的上下文记忆，关键业务条件应该在代码和数据库查询层进行约束。

---

# 七、AI 在数字方面的风险

项目中对 AI 数据助手的一个核心限制是：

> **LLM 不应该自己计算或者猜测业务数字。**

例如用户询问：

```text
牛肉Poke在六月的销售额是多少？
```

正确流程应该是：

```text
自然语言
   ↓
识别商品、时间、指标
   ↓
SQL 查询
   ↓
SUM(amount)
   ↓
数据库结果
   ↓
LLM解释
```

而不是：

```text
自然语言
   ↓
LLM 根据已有信息猜数字
```

因此开发过程中，对 AI 返回的关键数字会通过数据库查询进行验证。

---

# 八、哪些部分由我自己决定

以下部分没有完全依赖 AI，而是由人工确定：

### 1. 数据可信性设计

最终确定：

```text
LLM负责理解和解释
数据库负责提供事实
```

原因是业务数据不能依赖模型生成。

### 2. 数据清洗策略

原始数据存在不同类型的数据质量问题，因此先进行数据审计，再确定具体清洗方式，而不是让 AI 在没有检查数据的情况下直接修改数据。

### 3. 客单价定义

项目中采用：

```text
客单价 = 营业额 / 订单量
```

这是业务指标定义，需要结合题目和数据结构确定。

### 4. 多轮上下文问题的修复

AI 最初可以帮助定位问题，但最终确认“饮品问题”属于上下文实体继承错误，并确定修改查询条件的方式，是通过实际测试和数据库验证完成的。

### 5. 技术选型

SQLite、FastAPI、React 等技术选型主要根据本项目规模、开发效率和部署复杂度确定。

---

# 九、使用 AI 后的最终原则

这次开发过程中，我对 AI 编程的理解是：

```text
AI ≠ 自动完成项目

AI = 开发效率工具
```

AI 可以帮助：

* 快速生成代码
* 分析错误
* 提供实现方案
* 辅助调试
* 提高开发速度

但以下内容不能直接相信 AI：

* 数据结果
* 业务规则
* SQL 查询结果
* 统计数字
* 边界条件
* 多轮上下文逻辑

因此最终采用：

```text
AI生成
   ↓
人工检查
   ↓
运行测试
   ↓
数据库验证
   ↓
发现问题
   ↓
修改代码
   ↓
再次验证
```

而不是：

```text
AI生成
   ↓
直接提交
```

这也是本项目中使用 AI 辅助开发时最重要的原则。


**唐老板，咱们还没有电话沟通，七夕过完了，咱们抽个空沟通一下吧，括弧  性取向正常  括弧**
